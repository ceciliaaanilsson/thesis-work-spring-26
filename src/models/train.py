"""TODO: Model training module.

- Define a factory that builds the chosen ML models
- Include scaling pipelines where needed
- Train each model on the provided dataset
- Return a mapping of model name to trained instance
"""
